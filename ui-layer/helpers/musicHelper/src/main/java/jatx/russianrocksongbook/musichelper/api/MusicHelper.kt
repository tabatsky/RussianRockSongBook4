package jatx.russianrocksongbook.musichelper.api

interface MusicHelper {
    fun openYandexMusic(searchFor: String)
    fun openVkMusic(searchFor: String)
    fun openYoutubeMusic(searchFor: String)
}